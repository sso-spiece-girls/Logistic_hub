# Core (stdlib): tkinter (included with Python, install python3-tk on Debian/Ubuntu)
# Optional dependencies for enhanced features:
Pillow>=10.0.0       # Custom icon support (.png, .ico, .jpg)
pystray>=0.19.0      # System tray icon (Windows only)
requests>=2.28.0     # Favicon fetching for website cards
pytest>=7.0.0        # Testing
pyinstaller>=6.0.0   # Windows .exe build (optional)
